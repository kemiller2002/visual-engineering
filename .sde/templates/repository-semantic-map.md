# Repository Semantic Map

Keep this file small. It routes work to semantic areas and manifests; it does
not restate their rules. The default project location is `SDE-MAP.md`, unless
an existing top-level architecture/index document already provides the same
route and is linked unambiguously from the root README or agent instructions.

| Semantic area / feature | Purpose | Location | Manifest | Notes |
|---|---|---|---|---|
| `<name>` | `<one sentence>` | `<path>` | `<path>/manifest.md` | `<optional>` |

## Repository-wide composition

- Composition/root entry point: `<path or none — reason>`
- Shared contracts: `<paths or none — reason>`
- Architecture checks: `<commands/paths or none — reason>`
- Boundary checks: `<commands/paths or none — reason>`

## Areas without separate manifests

| Area | Reason a separate manifest is not needed |
|---|---|
| `<small/obvious area>` | `<reason>` |

