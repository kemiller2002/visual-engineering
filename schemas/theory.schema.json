{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/ros/theory.schema.json",
  "title": "ROS Theory",
  "allOf": [{"$ref": "artifact-metadata.schema.json"}],
  "type": "object",
  "required": ["id", "title", "status", "confidence"],
  "properties": {
    "id": {"pattern": "^TH-"},
    "status": {"enum": ["candidate", "supported", "established", "challenged", "superseded", "rejected"]}
  }
}
